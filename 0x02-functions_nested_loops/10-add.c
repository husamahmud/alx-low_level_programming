#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @a: param
 * @b: param
 *
 * Description: program that adds two integers and returns the result
 *
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	return (a + b);
}
