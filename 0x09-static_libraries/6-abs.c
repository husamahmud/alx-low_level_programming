#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @n: the character to check
 *
 * Description: program that computes the absolute value of an integer
 *
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	return (n > 0 ? n : -n);
}
